# ─── Payment Gateway — Variáveis de Ambiente ─────────────────────────────────
# Copie para .env e ajuste os valores

DB_HOST=localhost
DB_PORT=5432
DB_NAME=payments
DB_USER=postgres
DB_PASSWORD=SuaSenhaSegura123

RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# Taxa de falha do adquirente simulado (0.0 = nunca falha, 1.0 = sempre falha)
ACQUIRER_FAILURE_RATE=0.3
ACQUIRER_TIMEOUT_MS=2000

PORT=8080
